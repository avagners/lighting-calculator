# 📦 Руководство по версионированию

В проекте используется **Semantic Versioning** (SemVer) с автоматической генерацией CHANGELOG.

## 🏷️ Формат версий

Версии имеют формат: **MAJOR.MINOR.PATCH** (например, `1.0.0`)

- **MAJOR** — ломающие изменения (breaking changes)
- **MINOR** — новые функции (обратно совместимые)
- **PATCH** — исправления багов (обратно совместимые)

## 🚀 Создание релиза

### Автоматически (рекомендуется)

```bash
# Патч-версия (1.0.0 → 1.0.1)
npm run release

# Минорная версия (1.0.0 → 1.1.0)
npm run release:minor

# Мажорная версия (1.0.0 → 2.0.0)
npm run release:major

# Конкретная версия
npm run release:patch
```

### Что делает скрипт:

1. ✅ Обновляет версию в `package.json`
2. ✅ Генерирует `CHANGELOG.md` на основе коммитов
3. ✅ Создаёт git commit с сообщением `chore(release): X.Y.Z`
4. ✅ Создаёт git тег `vX.Y.Z`

### После релиза:

```bash
# Пуш коммита и тега в GitHub
git push --follow-tags origin main
```

## 📝 Формат коммитов

Для автоматической генерации CHANGELOG используйте [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<scope>): <description>
```

### Типы коммитов:

| Тип | Описание | В CHANGELOG |
|-----|----------|-------------|
| `feat` | Новая функция | ✨ Features |
| `fix` | Исправление бага | 🐛 Bug Fixes |
| `perf` | Улучшение производительности | ⚡ Performance |
| `revert` | Откат изменений | ⏪ Reverts |
| `docs` | Документация | 📝 Documentation |
| `refactor` | Рефакторинг | ♻️ Code Refactoring |
| `test` | Тесты | ✅ Tests |
| `build` | Сборка/зависимости | 📦 Build System |
| `style` | Стили/форматирование | 🎨 Styles (скрыто) |
| `chore` | Разное | 🔧 Chores (скрыто) |
| `ci` | CI/CD | 👷 CI/CD (скрыто) |

### Примеры:

```bash
# Новая функция
git commit -m "feat: добавить расчёт стоимости электроэнергии"

# Исправление бага
git commit -m "fix: исправить расчёт для комнат с высотой > 4м"

# Документация
git commit -m "docs: обновить README"

# Рефакторинг
git commit -m "refactor: упростить логику расчёта коэффициентов"

# Тесты
git commit -m "test: добавить тесты для useLightingCalculator"

# Breaking change (указать в теле коммита)
git commit -m "feat: изменить формулу расчёта

BREAKING CHANGE: формула теперь использует другой коэффициент
```

## 📋 CHANGELOG

Файл `CHANGELOG.md` генерируется автоматически и содержит:

- Все изменения по версиям
- Ссылки на коммиты
- Ссылки на сравнение версий (compare)

## 🔄 Полный цикл релиза

```bash
# 1. Убедиться что все тесты проходят
npm run test:run

# 2. Собрать проект
npm run build

# 3. Создать релиз
npm run release

# 4. Запушить в GitHub
git push --follow-tags origin main

# 5. (Опционально) Создать релиз на GitHub
# Перейти на https://github.com/avagners/lighting-calculator/releases
```

## 🎯 Примеры сценариев

### Сценарий 1: Исправление бага

```bash
git add .
git commit -m "fix: исправить ошибку расчёта для малых помещений"
npm run release  # 1.0.0 → 1.0.1
git push --follow-tags origin main
```

### Сценарий 2: Новая функция

```bash
git add .
git commit -m "feat: добавить калькулятор стоимости электроэнергии"
npm run release:minor  # 1.0.1 → 1.1.0
git push --follow-tags origin main
```

### Сценарий 3: Breaking changes

```bash
git add .
git commit -m "feat: новая формула расчёта

BREAKING CHANGE: изменён API функции calculateRequiredLumens"
npm run release:major  # 1.1.0 → 2.0.0
git push --follow-tags origin main
```

## 🔗 Ссылки

- [Semantic Versioning 2.0.0](https://semver.org/lang/ru/)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [standard-version](https://github.com/conventional-changelog/standard-version)
- [GitHub Releases](https://github.com/avagners/lighting-calculator/releases)
